version: '3.8'

# ============================================================
# HyperSpace AGI v1.01 — docker-compose
# Ollama: auto-detect NVIDIA GPU, AMD ROCm, Intel Arc/iGPU,
#         Vulkan, NPU (via env flag) — CPU fallback always on
# Dashboard:   http://localhost:8085/dashboard
# Open WebUI:  http://localhost:3000
# ============================================================

services:

  # --------------------
  # EVENT BUS
  # --------------------
  nats:
    image: nats:latest
    container_name: hyperspace_nats
    ports:
      - "4222:4222"
    networks:
      - hyperspace
    restart: unless-stopped

  # --------------------
  # OLLAMA — LLM locale
  # Accelerazione: usa il profilo che corrisponde al tuo HW.
  # Default (nessun profilo): CPU only.
  # Avvio con GPU:
  #   NVIDIA  → docker compose --profile nvidia up
  #   AMD     → docker compose --profile amd up
  #   Intel   → docker compose --profile intel up
  #   Vulkan  → docker compose --profile vulkan up
  # --------------------

  # CPU fallback — sempre attivo (nessun profilo richiesto)
  ollama:
    image: ollama/ollama:latest
    container_name: hyperspace_ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama
    environment:
      OLLAMA_NUM_THREADS: "0"
    restart: unless-stopped
    networks:
      - hyperspace

  # NVIDIA GPU (richiede nvidia-container-toolkit)
  ollama-nvidia:
    image: ollama/ollama:latest
    container_name: hyperspace_ollama_nvidia
    profiles: ["nvidia"]
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama
    environment:
      OLLAMA_NUM_GPU: "999"
      NVIDIA_VISIBLE_DEVICES: "all"
      NVIDIA_DRIVER_CAPABILITIES: "compute,utility"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    restart: unless-stopped
    networks:
      - hyperspace

  # AMD GPU — ROCm (richiede driver ROCm installati sull'host)
  ollama-amd:
    image: ollama/ollama:rocm
    container_name: hyperspace_ollama_amd
    profiles: ["amd"]
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama
    devices:
      - /dev/kfd:/dev/kfd
      - /dev/dri:/dev/dri
    group_add:
      - video
      - render
    environment:
      HSA_OVERRIDE_GFX_VERSION: "11.0.0"
      OLLAMA_NUM_GPU: "999"
    restart: unless-stopped
    networks:
      - hyperspace

  # Intel Arc / iGPU — via OpenCL / oneAPI
  ollama-intel:
    image: ollama/ollama:latest
    container_name: hyperspace_ollama_intel
    profiles: ["intel"]
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama
    devices:
      - /dev/dri:/dev/dri
    group_add:
      - render
      - video
    environment:
      OLLAMA_NUM_GPU: "999"
      ZES_ENABLE_SYSMAN: "1"
      SYCL_CACHE_PERSISTENT: "1"
    restart: unless-stopped
    networks:
      - hyperspace

  # Vulkan
  ollama-vulkan:
    image: ollama/ollama:latest
    container_name: hyperspace_ollama_vulkan
    profiles: ["vulkan"]
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama
    devices:
      - /dev/dri:/dev/dri
    group_add:
      - render
      - video
    environment:
      OLLAMA_NUM_GPU: "999"
      GGML_VULKAN: "1"
    restart: unless-stopped
    networks:
      - hyperspace

  # --------------------
  # OPEN WEBUI
  # Interfaccia grafica per chattare con Ollama
  # http://localhost:3000
  # --------------------
  open-webui:
    image: ghcr.io/open-webui/open-webui:main
    container_name: hyperspace_open_webui
    depends_on:
      - ollama
    ports:
      - "3000:8080"
    volumes:
      - open_webui_data:/app/backend/data
    environment:
      OLLAMA_BASE_URL: http://ollama:11434
      WEBUI_SECRET_KEY: hyperspace-secret-change-me
    networks:
      - hyperspace
    restart: unless-stopped

  # --------------------
  # AUTHORITY
  # --------------------
  authority:
    build:
      context: .
      dockerfile: authority/Dockerfile
    container_name: hyperspace_authority
    ports:
      - "8080:8080"
    environment:
      PYTHONUNBUFFERED: "1"
    networks:
      - hyperspace
    restart: unless-stopped

  # --------------------
  # CONTROL PLANE  (dashboard: http://localhost:8085/dashboard)
  # --------------------
  control-plane:
    build:
      context: .
      dockerfile: control-plane/Dockerfile
    container_name: hyperspace_control_plane
    depends_on:
      - nats
      - authority
      - ollama
    ports:
      - "8085:8085"
    environment:
      PYTHONUNBUFFERED: "1"
      AUTHORITY_URL: http://authority:8080
      OLLAMA_URL: http://ollama:11434
      OLLAMA_MODEL: phi3
    networks:
      - hyperspace
    restart: unless-stopped

  # --------------------
  # WORKER NODE
  # --------------------
  worker:
    build:
      context: .
      dockerfile: worker/Dockerfile
    container_name: hyperspace_worker_1
    depends_on:
      - nats
      - authority
      - ollama
    ports:
      - "8084:8084"
    environment:
      WORKER_ID: worker-1
      WORKER_PORT: "8084"
      AUTHORITY_URL: http://authority:8080
      OLLAMA_URL: http://ollama:11434
      OLLAMA_MODEL: phi3
      PYTHONUNBUFFERED: "1"
    networks:
      - hyperspace
    restart: unless-stopped

networks:
  hyperspace:
    driver: bridge

volumes:
  ollama_data:
  open_webui_data:
