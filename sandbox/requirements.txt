# Runtime/test subset shared by HyperSpace's local Python services.
# Cloud connectors and GUI installer packages stay outside the offline runner.
cryptography
fastapi>=0.111.0
flask==3.0.3
flask-cors
httpx>=0.27.0
pytest
requests==2.32.3
uvicorn[standard]>=0.29.0
